<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="sentinelStreamPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>com.sentinelstream.persistence.jpa.IncidentReport</class>
        <class>com.sentinelstream.persistence.jpa.ThreatOrigin</class>

        <properties>
            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/sentinel_stream_db"/>
            <property name="javax.persistence.jdbc.user" value="postgres"/>
            <property name="javax.persistence.jdbc.password" value="your_secure_password"/>

            <!-- Hibernate creates/updates IncidentReport + ThreatOrigin tables automatically,
                 per the README's "JPA framework will automatically generate the schema" step.
                 The raw_telemetry table is NOT managed here; it is created by sql/init.sql,
                 since it is written via pure JDBC and intentionally kept outside ORM control. -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Hibernate's own internal connection pool is fine here since the JDBC
                 fast-path uses its own separate Singleton pool (DatabaseConnectionManager)
                 for raw telemetry writes; the two paths do not share connections. -->
            <property name="hibernate.connection.pool_size" value="5"/>
        </properties>
    </persistence-unit>
</persistence>
